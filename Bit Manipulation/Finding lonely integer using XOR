#include<stdio.h>
int main()
{
    int n,m,count=0,result=0;
    scanf("%d",&n);
    int a[n];
    int i,j;
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        result^=a[i];
    }
    printf("%d",result);
    return 0;
    
}

